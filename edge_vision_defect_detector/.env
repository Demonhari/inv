# if you haven’t initialised Poetry yet
poetry init  # then overwrite with the TOML seen above, or just run the next two lines

# install everything (+ dev extras) into a virtualenv managed by Poetry
poetry install --with dev

# activate the environment for your shell session
poetry shell
